CREATE TYPE [dbo].[tbltype_sale_activities] AS TABLE (
    [Id]            INT            NULL,
    [Value]         VARCHAR (150)  NULL,
    [Comment]       VARCHAR (1000) NULL,
    [ScheduledDate] DATETIME       NULL);

